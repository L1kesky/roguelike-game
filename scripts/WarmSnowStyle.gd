extends Node
class_name WarmSnowStyle

# ===== 暖雪·水墨古风 色彩体系 =====
# 背景
const BG_DARK := Color(0.04, 0.02, 0.02)
const BG_PANEL := Color(0.08, 0.05, 0.03, 0.92)

# 血
const BLOOD_RED := Color(0.75, 0.08, 0.08)
const BLOOD_DARK := Color(0.45, 0.04, 0.04)
const BLOOD_CRIMSON := Color(0.55, 0.05, 0.05)
const BLOOD_SPLATTER := Color(0.5, 0.03, 0.03, 0.18)

# 文字
const TEXT_WARM := Color(0.92, 0.82, 0.68)
const TEXT_BRIGHT := Color(0.98, 0.94, 0.85)

# 金色
const GOLD := Color(0.92, 0.72, 0.28)
const GOLD_DIM := Color(0.72, 0.52, 0.12)
const GOLD_GLOW := Color(0.92, 0.72, 0.28, 0.3)
const GOLD_DARK := Color(0.4, 0.25, 0.12)

# 怒气
const RAGE_ORANGE := Color(0.95, 0.55, 0.15)
const RAGE_BAR := Color(0.85, 0.35, 0.08)

# 飞剑
const FLYING_SWORD := Color(0.65, 0.75, 0.95)
const TRACKING_SWORD := Color(0.95, 0.75, 0.35)
const PIERCE_SWORD := Color(0.55, 0.85, 0.95)
const SPIRIT_SWORD := Color(0.85, 0.65, 0.95)

# 敌人
const RING_RED := Color(0.55, 0.05, 0.05)
const ELITE_PURPLE := Color(0.55, 0.20, 0.55)
const BOSS_GOLD := Color(0.85, 0.65, 0.15)
const ENEMY_GLOW := Color(0.6, 0.3, 0.15, 0.2)

# 地图
const FLOOR_DARK := Color(0.14, 0.09, 0.06)
const FLOOR_LINE := Color(0.18, 0.12, 0.08)
const WALL_DARK := Color(0.18, 0.12, 0.08)
const WALL_BASE := Color(0.25, 0.16, 0.09)
const WALL_BRIGHT := Color(0.35, 0.22, 0.12)
const STAIRS_GLOW := Color(0.7, 0.6, 0.3)
const DARK_TILE := Color(0.06, 0.04, 0.03)
const SHADOW_LINE := Color(0.15, 0.08, 0.04)

# 物品
const HEAL_GREEN := Color(0.25, 0.85, 0.35)
const WEAPON_BLUE := Color(0.65, 0.75, 0.95)
const SCROLL_WARM := Color(0.92, 0.82, 0.68)
const SHIELD_BLUE := Color(0.3, 0.5, 1.0)

# 玩家
const PLAYER_BODY := Color(0.04, 0.02, 0.02)
const PLAYER_OUTLINE := Color(0.75, 0.08, 0.08)
const PLAYER_GLOW := Color(0.75, 0.08, 0.08, 0.12)
const PLAYER_SWORD := Color(0.4, 0.25, 0.12)
const PLAYER_HAIR := Color(0.2, 0.15, 0.12)
const PLAYER_MASK := Color(0.85, 0.82, 0.78)

# 特效
const CRIT_YELLOW := Color(1.0, 0.9, 0.4)
const CRIT_WHITE := Color(1.0, 1.0, 0.8)
const RAGE_CORE := Color(0.95, 0.1, 0.05)
const RAGE_MID := Color(0.95, 0.55, 0.15)
const RAGE_OUTER := Color(0.95, 0.35, 0.1)
const LIGHTNING_CORE := Color(0.6, 0.7, 1.0)
const LIGHTNING_GLOW := Color(0.8, 0.85, 1.0)
const HEAL_CORE := Color(0.25, 0.85, 0.35)
const HEAL_GLOW := Color(0.25, 0.95, 0.45)